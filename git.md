下面给你一套**标准、面试级、命令行提交修改文件的完整流程**。
不管你改了 1 个文件还是 100 个文件，都适用。

---

## ✅ 一、最标准的提交流程（通用）

```bash
# 1. 查看哪些文件被修改
git status

# 2. 把修改加入暂存区
git add 文件名
# 或一次性全部加入
git add .

# 3. 提交到本地仓库(commit是注释)
git commit -m "your commit message"

# 4. 推送到远端
git push
```

---

## ✅ 二、常见几种“精准提交”方式（很重要）

### 1️⃣ 只提交某一个文件（面试很加分）

```bash
git add src/main.cpp
git commit -m "fix grpc connection bug"
```

---

### 2️⃣ 只提交某一类文件

```bash
git add *.cpp
git commit -m "update cpp files"
```

---

### 3️⃣ 交互式提交（推荐进阶）

```bash
git add -p
```

你可以逐块（hunk）选择：

* `y`：加入
* `n`：不加入
* `s`：拆分
* `q`：退出

👉 **面试官很喜欢听到这个**

---

## ✅ 三、修改后直接提交（快捷但要慎用）

```bash
git commit -am "fix bug"
```

⚠️ 注意：

* **只能提交已被 git 跟踪过的文件**
* 新文件不会被提交

---

## ✅ 四、提交后如何确认成功？

```bash
git log --oneline -3
```

你会看到类似：

```text
a1b2c3d fix grpc connection bug
```

---

## ✅ 五、如果你改错了，怎么补救（面试常问）

### 1️⃣ 改了文件但还没 add

```bash
git checkout -- 文件名
```

---

### 2️⃣ 已 add，但还没 commit

```bash
git reset HEAD 文件名
```

---

### 3️⃣ 已 commit，但还没 push

```bash
git commit --amend
```

---

## 🧠 面试级一句话总结

> “命令行提交流程是 `git status → git add → git commit → git push`，可以通过 `git add -p` 精细控制提交粒度，保证提交原子性。”

---

## 🔥 给你一个“实战模板”（你可以直接用）

```bash
git status
git add .
git commit -m "update grpc client logic"
git push
```

如果你愿意，我可以 **模拟一个面试官**，问你几道 Git 实战追问（比如 *commit amend / reset / rebase*），帮你把 Git 这块彻底吃透。
